# ============================================================
# Fibonacci.action —— 自定义动作接口定义
# 功能：客户端指定阶数，服务端计算斐波那契数列并实时反馈进度
#
# 格式说明：
#   --- 第一段是"目标(Goal)"部分（客户端发送的任务参数）
#   --- 第二段是"结果(Result)"部分（任务完成后的最终返回值）
#   --- 第三段是"反馈(Feedback)"部分（任务执行过程中的实时进度）
# ============================================================

int32 order              # 目标：要计算的斐波那契数列阶数（项数）
---
int32[] sequence         # 结果：完整的斐波那契数列（任务结束后返回）
---
int32[] partial_sequence # 反馈：当前已计算出的部分序列（周期性反馈进度）
